# Pin the Django LTS line. 4.2 keeps Python 3.8+ support; bump to 5.x when
# you drop legacy Python.
Django>=4.2,<5.2

# Async task queue (the existing Celery wiring assumes the django-db result backend).
celery>=5.3,<6
django-celery-results>=2.5,<3

# Recommender / crawler dependencies.
pandas>=2.0,<3
scikit-learn>=1.3
matplotlib>=3.7
beautifulsoup4>=4.12
datefinder>=0.7

# gensim is only used by the offline crawler (Make_DB / keyword extraction).
# Pinned loosely; building wheels can be touchy on bleeding-edge Pythons.
gensim>=4.3
